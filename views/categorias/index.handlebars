<h2>Categorias:</h2>
<hr>
<div class="card mt-4">
    <div class="card-body">
        {{#each categorias}}
            <a href="/categories/{{slug}}"><h4>{{nome}}</h4></a>
        {{else}}
            <h4 class="mt-3">Nenhuma categoria registrada :(</h4>
        {{/each}}
    </div>
</div>