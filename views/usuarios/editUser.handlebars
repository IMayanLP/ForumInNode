<div class="row">
    <div class="edit">
      <h1>Editar perfil ou</h1>
    </div>
    <div class="edit2">
      <!-- Botão para acionar modal -->
<button type="button" class="btn btn-danger btn-lg" data-toggle="modal" data-target="#modalExemplo">Deletar conta</button>
      {{!-- <form action="/user/delete" method="post">
        <input type="hidden" name="id" value="{{usuario._id}}">
        <input type="submit" value="Deletar conta" class="btn btn-danger btn-lg" onclick="confirm('Esta conta será excluida')">
      </form> --}}
    </div>
</div>

<!-- Modal -->
<div class="modal fade" id="modalExemplo" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Excluir conta</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Fechar">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Essa conta será excluida permanetemente, deseja continuar?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
        <form action="/user/delete" method="post">
        <input type="hidden" name="id" value="{{usuario._id}}">
        <input type="submit" value="Deletar conta" class="btn btn-danger">
      </form>
      </div>
    </div>
  </div>
</div>
<form class="bg-dark box" action="/user/edit" method="post">
    {{#if error}}
        <div class="alert alert-danger">{{error}}</div>
    {{else}}
    {{/if}}
  <input type="hidden" name="id" value="{{usuario._id}}">
  <input type="email" name="email" placeholder="Digite o novo email" value="{{usuario.email}}" required>
  <input type="text" name="nome" placeholder="Digite o novo nome" value="{{usuario.nome}}" required>
  <input type="password" name="senha" placeholder="Digite a nova senha" required>
  <input type="password" name="senha2" placeholder="Repita a senha" required>
  <input type="submit" name="cadastrar" value="Salvar">
</form>